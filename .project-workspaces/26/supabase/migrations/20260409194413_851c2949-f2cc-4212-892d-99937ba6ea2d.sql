
DROP POLICY "Allow direct vision image deletes" ON storage.objects;
DROP POLICY "Allow direct vision image updates" ON storage.objects;
DROP POLICY "Allow direct vision image uploads" ON storage.objects;
