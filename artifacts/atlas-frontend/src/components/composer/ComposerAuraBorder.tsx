// Replaced by composerAura.ts — the animation now lives directly on the
// composer element's ::before pseudo-element via the `atlas-composer-live`
// CSS class. No separate component or wrapper div is needed.
export {};
